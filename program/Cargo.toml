[package]
name = "arb-program"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
pinocchio = "0.10"
dex_pinocchio_cpi = { path = "../dex-pinocchio-cpi" }
five8_const = "0.1"

[features]
default = []
no-entrypoint = []
test-utils = []
